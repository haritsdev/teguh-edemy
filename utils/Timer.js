import React from 'react';

const Timer = (setProgress, uploading) => {
  if (uploading === true) {
    return setTimeout(() => setProgress(10), 500);
  } else {
    setTimeout(() => setProgress(1), 500);
    setTimeout(() => setProgress(2), 800);
    setTimeout(() => setProgress(3), 1000);
    setTimeout(() => setProgress(4), 1500);
    setTimeout(() => setProgress(5), 2000);
    setTimeout(() => setProgress(6), 2100);
    setTimeout(() => setProgress(7), 2300);
    setTimeout(() => setProgress(8), 2400);
    setTimeout(() => setProgress(9), 2700);
    setTimeout(() => setProgress(10), 3500);
    setTimeout(() => setProgress(11), 4000);
    setTimeout(() => setProgress(12), 4500);
    setTimeout(() => setProgress(13), 5000);
    setTimeout(() => setProgress(14), 5500);
    setTimeout(() => setProgress(16), 6500);
    setTimeout(() => setProgress(17), 7000);
    setTimeout(() => setProgress(18), 8000);
    setTimeout(() => setProgress(19), 12000);
    setTimeout(() => setProgress(20), 13000);
    setTimeout(() => setProgress(21), 13300);
    setTimeout(() => setProgress(22), 13500);
    setTimeout(() => setProgress(23), 13700);
    setTimeout(() => setProgress(24), 14000);
    setTimeout(() => setProgress(25), 14500);
    setTimeout(() => setProgress(26), 15500);
    setTimeout(() => setProgress(27), 17000);
    setTimeout(() => setProgress(28), 17500);
    setTimeout(() => setProgress(29), 18000);
    setTimeout(() => setProgress(30), 18500);
    setTimeout(() => setProgress(31), 18700);
    setTimeout(() => setProgress(32), 19300);
    setTimeout(() => setProgress(33), 19500);
    setTimeout(() => setProgress(34), 19800);
    setTimeout(() => setProgress(35), 20100);
    setTimeout(() => setProgress(36), 21500);
    setTimeout(() => setProgress(37), 22000);
    setTimeout(() => setProgress(38), 22500);
    setTimeout(() => setProgress(40), 23000);
    setTimeout(() => setProgress(41), 23500);
    setTimeout(() => setProgress(42), 24000);
    setTimeout(() => setProgress(43), 24500);
    setTimeout(() => setProgress(44), 25000);
    setTimeout(() => setProgress(45), 25500);
    setTimeout(() => setProgress(50), 26000);
    setTimeout(() => setProgress(51), 26500);
    setTimeout(() => setProgress(52), 27000);
    setTimeout(() => setProgress(53), 28000);
    setTimeout(() => setProgress(54), 29000);
    setTimeout(() => setProgress(55), 30300);
    setTimeout(() => setProgress(56), 30600);
    setTimeout(() => setProgress(57), 31000);
    setTimeout(() => setProgress(58), 32500);
    setTimeout(() => setProgress(59), 33000);
    setTimeout(() => setProgress(60), 33500);
    setTimeout(() => setProgress(61), 33700);
    setTimeout(() => setProgress(62), 34000);
    setTimeout(() => setProgress(63), 34500);
    setTimeout(() => setProgress(64), 34700);
    setTimeout(() => setProgress(65), 35000);
    setTimeout(() => setProgress(66), 35500);
    setTimeout(() => setProgress(67), 36000);
    setTimeout(() => setProgress(68), 36500);
    setTimeout(() => setProgress(69), 36700);
    setTimeout(() => setProgress(70), 38000);
    setTimeout(() => setProgress(73), 38500);
    setTimeout(() => setProgress(75), 39000);
    setTimeout(() => setProgress(76), 39500);
    setTimeout(() => setProgress(77), 40000);
    setTimeout(() => setProgress(78), 40500);
    setTimeout(() => setProgress(79), 41000);
    setTimeout(() => setProgress(80), 40500);
    setTimeout(() => setProgress(81), 41000);
    setTimeout(() => setProgress(82), 41500);
    setTimeout(() => setProgress(83), 42000);
    setTimeout(() => setProgress(84), 42500);
    setTimeout(() => setProgress(85), 43000);
    setTimeout(() => setProgress(86), 43500);
    setTimeout(() => setProgress(87), 44000);
    setTimeout(() => setProgress(88), 45000);
    setTimeout(() => setProgress(89), 45500);
    setTimeout(() => setProgress(90), 47000);
    setTimeout(() => setProgress(93), 50000);
    setTimeout(() => setProgress(94), 60000);
    // setTimeout(() => setProgress(95), 65000);
    // setTimeout(() => setProgress(97), 70000);
    // setTimeout(() => setProgress(98), 80500);
    // setTimeout(() => setProgress(99), 85000);
  }
};

export default Timer;
